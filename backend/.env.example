# Copy to .env in project root or backend/ and adjust.

SECRET_KEY=replace-with-long-random-string
JWT_SECRET_KEY=replace-with-long-random-string


# SQLite (default) — or PostgreSQL:

# JWT lifetime (hours)
# JWT_ACCESS_HOURS=24

# Server port
# PORT=5000
